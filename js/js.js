var rating=document.querySelector('.rating'),
ratingitem=document.querySelectorAll('.rating-item');
rating.oncklick= function(e){
    var target = e.target;
    if(target.classList.contains('rating-item')){
        
    }
}